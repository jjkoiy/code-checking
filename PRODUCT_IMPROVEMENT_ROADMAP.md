# Agent Review System 产品改进路线图

## 1. 产品定位

Agent Review System 当前已经具备一个可运行的代码审查闭环：

```text
提交 Git diff -> 创建审查任务 -> 多 Agent 顺序审查 -> 生成结构化 findings -> 输出 Markdown / JSON 报告
```

建议下一阶段将产品定位聚焦为：

> 面向中小研发团队的 PR 自动审查助手，优先发现安全风险、明显代码缺陷、风格问题和测试缺口，帮助人工 reviewer 降低重复劳动。

短期不要做成大而全的 AI 编程平台。先把 PR 审查前置质检做扎实，再逐步扩展到测试生成、自动修复、团队知识库和研发流程集成。

## 2. 当前可用能力

当前系统可以支持以下核心场景：

- 用户通过 API 或 Web UI 提交标准 Git diff。
- 系统异步创建并执行代码审查任务。
- 多个 Agent 顺序执行上下文构建、静态检查、风格检查、安全扫描、测试影响分析、LLM 审查、测试草稿生成、结果校验和报告生成。
- 审查结果以结构化 JSON 和 Markdown 报告返回。
- 报告中包含问题级别、文件路径、行号、证据、建议、置信度和是否阻塞。
- 系统支持 mock LLM 模式，便于本地开发和测试。

## 3. 核心问题

当前项目更接近 P1 原型，主要短板包括：

- 输入方式较弱，主要依赖手动粘贴 diff。
- 规则检查仍偏基础，误报和漏报控制还不够强。
- LLM 输出缺少更严格的 schema 校验、敏感信息脱敏和外发策略控制。
- Web UI 只是基础控制台，还不是完整的审查工作台。
- FastAPI `BackgroundTasks` 不适合生产级长任务调度。
- 缺少用户、团队、仓库、权限和审计能力。
- 测试生成仍是草稿，没有执行闭环。
- 缺少产品指标和可观测性数据。

## 4. 改进方向总览

| 方向 | 目标 | 优先级 |
|---|---|---|
| 输入能力 | 接入真实 PR / 分支 / 仓库上下文 | 高 |
| 审查质量 | 降低误报，提升高价值 finding 比例 | 高 |
| LLM 安全 | 控制代码外发、校验模型输出 | 高 |
| 报告体验 | 从报告展示升级为审查工作台 | 中高 |
| 流程集成 | 进入 PR、CI/CD、CLI 工作流 | 中高 |
| 任务可靠性 | 支持持久任务、重试、超时和恢复 | 中高 |
| 权限治理 | 支持用户、团队、仓库隔离 | 中 |
| 测试闭环 | 从测试建议升级为可运行测试补丁 | 中 |
| 可观测性 | 看清审查质量、成本和用户采纳 | 中 |

## 5. 阶段规划

### 阶段一：可试用 MVP

目标：让真实研发团队可以在小范围 PR 场景中试用。

建议建设：

- 支持 GitHub / GitLab PR 输入。
- 支持按 `base_ref` 和 `head_ref` 自动生成 diff。
- 增加 LLM 外发开关和敏感信息脱敏。
- 对 LLM 输出增加 JSON Schema 校验和失败降级。
- 优化报告 UI，支持按严重级别、文件、类别筛选。
- 增加“是否建议合并”的总体结论。
- 增加高危和阻塞问题的独立展示。

验收标准：

- 用户无需手动复制 diff，即可对一个 PR 发起审查。
- 高危问题可以清晰显示在报告顶部。
- LLM 输出异常不会导致整条审查任务失败。
- 默认配置不会把代码发送到外部 LLM。

### 阶段二：团队协作版

目标：让审查结果可以进入团队日常协作流程。

建议建设：

- 支持自动评论到 PR。
- 支持问题状态：待处理、已忽略、已修复、误报。
- 支持审查历史记录和同一 PR 的多轮对比。
- 支持团队级规则配置。
- 支持仓库级风险策略，例如高危阻塞合并、中低危仅提示。
- 增加 CLI：`agent-review diff`、`agent-review pr`。
- 引入持久化任务队列，例如 RQ、Celery、Arq 或 Dramatiq。

验收标准：

- 团队可以在 PR 页面直接看到审查结论。
- 被标记为误报的问题不会在同类场景中持续干扰用户。
- 服务重启后 pending / running 任务不会静默丢失。
- 每个任务能看到执行阶段、耗时和失败原因。

### 阶段三：高质量审查引擎

目标：让系统从规则型检查器升级为可信的智能 reviewer。

建议建设：

- Python 规则逐步从正则迁移到 AST。
- 引入 Semgrep 或 Tree-sitter 支持多语言规则。
- 增加 JavaScript、TypeScript、Go、Java 的基础规则包。
- 增加依赖风险扫描，包括漏洞、许可证和危险包。
- 引入项目知识库，支持读取编码规范、架构文档和历史问题。
- 将历史误报、采纳情况、人工反馈用于规则优化。

验收标准：

- 常见 Python 误报明显下降。
- 每个规则都有正向和反向测试。
- Finding 可以解释“为什么这是风险”和“如何最小修复”。
- 审查结果能体现项目上下文，而不只是通用规则。

### 阶段四：测试与修复闭环

目标：从“指出问题”升级为“帮助完成修复验证”。

建议建设：

- 根据 finding 自动生成测试计划。
- 针对高危问题生成可运行测试草稿。
- 在隔离沙箱中执行生成测试。
- 将测试失败信息反馈到报告。
- 支持生成修复建议 patch。
- 支持用户选择性应用测试补丁或修复补丁。

验收标准：

- 高危 finding 至少有一个对应测试建议。
- 生成的测试能区分 `plan_only` 和 `runnable`。
- 报告中能看到测试是否执行、执行结果和失败原因。
- 用户可以基于报告快速补充回归测试。

## 6. 推荐优先级

建议先做以下 5 件事：

1. 接入 PR 输入，减少手动粘贴 diff 的使用门槛。
2. 增加 LLM 外发策略、脱敏和 schema 校验，守住安全底线。
3. 优化报告 UI，支持筛选、状态和合并建议。
4. 引入持久任务队列，提升长任务可靠性。
5. 用 AST / Semgrep 提升核心规则质量，减少误报。

不建议短期优先做：

- 大规模多语言全覆盖。
- 自动修复所有问题。
- 复杂的企业权限体系。
- 过早商业化计费。
- 过早做插件市场或大型平台化架构。

这些方向有价值，但应等 MVP 在真实 PR 场景中跑通并收集反馈后再推进。

## 7. 产品指标

建议从一开始记录以下指标：

| 指标 | 说明 |
|---|---|
| 审查任务数 | 每日 / 每周触发多少次审查 |
| 完成率 | 审查任务成功完成比例 |
| 平均耗时 | 从提交到报告生成的时间 |
| Finding 数量 | 每次审查发现的问题数量 |
| 高危发现率 | 高危 / 阻塞问题占比 |
| 误报率 | 用户标记为误报的问题占比 |
| 采纳率 | 用户实际修复的问题占比 |
| LLM 成本 | token、请求次数、平均费用 |
| 失败原因分布 | 哪些 Agent 或外部依赖最容易失败 |

这些指标可以帮助判断产品是不是真的在减少 review 成本，而不是制造更多噪音。

## 8. 目标用户与使用场景

### 目标用户

- 中小研发团队。
- 对安全和代码质量有要求，但没有完整 DevSecOps 工具链的团队。
- 希望在 PR 阶段提前发现明显问题的技术负责人。
- 想减少重复审查工作的 reviewer。

### 典型场景

- 开发者提交 PR 后自动触发审查。
- Reviewer 在人工审查前先查看自动报告。
- 高危安全问题直接阻塞合并。
- 中低危问题作为 PR 评论提醒。
- 测试缺口被转化为测试建议或测试草稿。

## 9. 一句话路线

短期做成“能接入 PR 的自动审查助手”，中期做成“团队代码质量工作台”，长期再演进为“带测试和修复闭环的智能研发质量平台”。
