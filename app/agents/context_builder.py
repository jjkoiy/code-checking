"""Context Builder Agent stub — P1."""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def parse_diff(diff_text: str) -> list[dict]:
    """Placeholder: parse git diff into structured changed_file entries."""
    # TODO: real diff parsing in P1
    return []
